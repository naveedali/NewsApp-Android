package sample.naveedali.newsapp.data.remote

/** Constants for building the Retrofit instance in the DI layer (Phase 4). */
object NewsApiConfig {
    const val BASE_URL = "https://newsapi.org/v2/"
}
